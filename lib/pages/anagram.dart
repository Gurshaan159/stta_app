import 'package:flutter/material.dart';

class AnagramPage extends StatelessWidget {
  const AnagramPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Anagram'),),
    );
  }
}